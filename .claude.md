# Shadobus - カードゲームプロジェクト

## プロジェクト概要
シャドウバース風のターン制カードゲーム。フォロワーカードを召喚し、相手リーダーの体力を0にすることが勝利条件。

### 主要機能
- フォロワーカードの召喚（5スロット制限）
- ドラッグ操作による攻撃システム
- PP（パワーポイント）システム
- 5種類のアビリティ（Rush, Storm, Ward, Bane, Drain）
- 300枚以上のカードデータベース

## 技術スタック

| 技術 | バージョン | 用途 |
|------|-----------|------|
| Next.js | 16.1.1 | App Router使用 |
| React | 19.2.3 | UI構築 |
| TypeScript | 5 | strict mode有効 |
| Zustand | 5.0.9 | 状態管理 |
| Immer | - | 不変更新 |
| Tailwind CSS | 4 | スタイリング |

## プロジェクト構造

```
src/
├── app/              # Next.js App Router
│   ├── page.tsx      # メインゲーム画面
│   └── cards/        # カード一覧ページ
├── components/game/  # ゲームUIコンポーネント
│   ├── board.tsx     # ゲームボード
│   ├── card.tsx      # カードコンポーネント
│   ├── hand.tsx      # 手札
│   ├── player-field.tsx    # プレイヤーフィールド
│   ├── follower-slot.tsx   # フォロワースロット
│   ├── attack-line.tsx     # 攻撃ライン描画
│   └── card-detail-panel.tsx # カード詳細
├── lib/game-logic/   # ゲームロジック
│   ├── combat.ts     # 戦闘処理
│   └── placement.ts  # 配置処理
├── store/
│   └── game-store.ts # Zustandストア
├── types/
│   ├── card.types.ts # カード型定義
│   └── game.types.ts # ゲーム型定義
└── data/
    └── sample-cards.ts # カードデータベース
```

## 状態管理アーキテクチャ

### Zustand + Immerパターン
不変性を保ちながら直感的なミューテーション構文を実現。

```typescript
// store/game-store.ts
const useGameStore = create<GameState>()(
  immer((set, get) => ({
    // 状態とアクション
  }))
)
```

### GameState構造
```typescript
interface GameState {
  phase: 'waiting' | 'playing' | 'ended'
  turnPhase: 'start' | 'main' | 'end'
  currentTurn: PlayerId          // 'player1' | 'player2'
  turnNumber: number
  board: BoardState              // 両プレイヤーのフィールド
  players: Record<PlayerId, PlayerState>
  selection: SelectionState      // 攻撃対象選択状態
  winner: PlayerId | null
}
```

### 主要アクションメソッド
- `startGame()` - ゲーム開始、デッキシャッフル、初期手札配布
- `playCard(playerId, cardIndex)` - カード召喚
- `executeAttack(attackerId, targetId)` - 攻撃実行
- `endTurn()` - ターン終了、PP回復、ドロー
- `resetGame()` - ゲームリセット

## ゲームロジック詳細

### 戦闘システム (`lib/game-logic/combat.ts`)
**同時ダメージ解決**: 攻撃者と防御者が同時にダメージを受ける。優先権による不公平を防止。

```typescript
// 攻撃可能判定
canFollowerAttack(follower, turnNumber): boolean
// Rush/Stormがない場合、召喚酔いチェック

// 有効な攻撃対象取得
getValidAttackTargets(attacker, opponentField, attackerHasStorm): string[]
// Ward持ちがいる場合、Ward持ちのみがターゲット可能
```

### アビリティシステム
| アビリティ | 効果 |
|-----------|------|
| Rush | 召喚ターンにフォロワーを攻撃可能 |
| Storm | 召喚ターンにリーダーも攻撃可能 |
| Ward | 自分がいる限り他のユニットへの攻撃を防ぐ |
| Bane | ダメージを与えた相手を即死させる |
| Drain | 与えたダメージ分、リーダーを回復 |

### PP（パワーポイント）システム
- ターン1: 1PP → ターン10以降: 10PP（上限）
- ターン開始時に最大値まで回復
- カード召喚でコスト分消費

### 召喚酔い
- 召喚直後のフォロワーは攻撃不可
- 次のターン開始で攻撃可能に
- Rush/Storm持ちは例外

## 型定義設計

### カード定義 vs インスタンスの分離

**FollowerCardDefinition** (テンプレート)
```typescript
interface FollowerCardDefinition {
  id: string              // 'shadow-reaper'
  name: string
  cost: number            // 0-10
  baseAttack: number
  baseHealth: number
  abilities: FollowerAbility[]
  description?: string
}
```

**FollowerInstance** (ゲーム内実体)
```typescript
interface FollowerInstance {
  instanceId: string      // UUID - 一意識別子
  definitionId: string    // カード定義への参照
  currentAttack: number   // バフ/デバフ適用後
  currentHealth: number
  maxHealth: number
  abilities: FollowerAbility[]
  canAttack: boolean
  hasAttacked: boolean
  turnPlayed: number      // 召喚酔い判定用
}
```

**設計理由**:
- 同じカードが複数枚場に出る可能性
- インスタンスごとに異なるステータス変化
- カード定義は不変、インスタンスは可変

## コンポーネント設計

### コンポーネント階層
```
page.tsx (GamePage)
├── Board
│   ├── PlayerField (x2)
│   │   ├── FollowerSlot (x5)
│   │   │   └── Card
│   │   └── Leader
│   └── AttackLine
├── Hand (x2)
│   └── Card
├── TurnTransitionOverlay
├── CardDetailPanel
└── LeaderDamageEffect
```

### インタラクションパターン
**ドラッグ攻撃**:
1. 自フォロワーで`mouseDown` → 攻撃者選択
2. 敵フォロワー/リーダーで`mouseUp` → 攻撃実行
3. 無効な場所で`mouseUp` → 選択解除

## 開発コマンド

```bash
npm run dev      # 開発サーバー起動 (localhost:3000)
npm run build    # プロダクションビルド
npm run start    # プロダクションサーバー起動
npm run lint     # ESLintチェック
```

## コーディング規約

### スタイリング
- Tailwind CSSを使用
- インラインスタイルは最小限に

### ゲームロジック
- 純粋関数として実装
- 副作用はストアアクション内に限定
- テスト可能な設計

### 状態管理
- Zustandセレクターで必要な状態のみ購読
- UIローカル状態とゲーム状態を分離
- Immerで不変更新

### TypeScript
- strict mode有効
- 型推論を活用しつつ、公開APIには明示的な型付け
- ユニオン型でアビリティを表現
